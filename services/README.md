# Services

FastAPI services live here.

Rules:
- Each service owns its data and API.
- Provide /health and /ready endpoints.
- OpenAPI spec must exist in contracts/.
- No shared database tables without explicit contract.
