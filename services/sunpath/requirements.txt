# SunPath service — pinned to the versions validated in SAT-226 smoke tests
# (pvlib NREL SPA + pandas 3 + numpy 2). Code is pandas-3-safe (freq='h', no
# local_time string reparsing). Bump deliberately, re-run tests/sunpath_smoke.py.

fastapi==0.136.3
uvicorn[standard]==0.48.0
pydantic==2.13.4
pydantic-settings==2.14.2

# Solar calculations (NREL SPA)
pvlib==0.15.1
pandas==3.0.3
numpy==2.4.6

# Timezone detection
tzfpy==1.3.1
pytz==2026.2

# Diagram rendering (headless: MPLBACKEND=Agg)
matplotlib==3.10.9

# Building extraction + GIS (GEE optional)
earthengine-api==1.7.28
geopy==2.4.1
shapely==2.1.2
pyproj==3.7.2
requests==2.34.2

# HTTP + config
httpx==0.28.1
python-dotenv==1.2.2

# Tests
pytest==9.0.3
pytest-asyncio==1.4.0
